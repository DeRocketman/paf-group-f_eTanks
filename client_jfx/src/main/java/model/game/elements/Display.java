package model.game.elements;

import javafx.scene.image.Image;

public class Display extends LevelElement{
    public Display(Image image, LevelElementType type, double positionX, double positionY, double width, double height, double rotation) {
        super(image, type, positionX, positionY, width, height, rotation);
    }
}
